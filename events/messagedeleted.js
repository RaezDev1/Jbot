module.exports = {
	name: 'ready',
	once: true,
	execute(client) {
       // client.on('messageDelete', async function(messageDelete) {
               // client.channels.fetch(testing.discordlogs)
               // .then(channel=>channel.send(`DeletedMessage: ${messageDelete} was deleted! by:`))
         // });
	},
};
