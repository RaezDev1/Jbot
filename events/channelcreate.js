module.exports = {
	name: 'ready',
	once: true,
	execute(client) {
    //    client.on('channelCreate', async function(channelCreate) {
       //     client.channels.fetch(testing.discordlogs)
       //     .then(channel=>channel.send(`ChannelCreated:  ${channelCreate.name} has just been created!`))
        //  });
	},
};
