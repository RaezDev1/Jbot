module.exports = {
	name: 'ready',
	once: true,
	execute(client, user) {
       // client.on('channelDelete', async function(channelDelete) {
           // client.channels.fetch(testing.discordlogs)
           // .then(channel=>channel.send(`ChannelDeleted: ${channelDelete.name} has just been deleted!`))
         // });
	},
};
